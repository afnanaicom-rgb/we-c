# تحليل التعديلات المطلوبة

## 1. إطار البروفايل (room.html)
### المشكلة:
- الإطار يحتاج أن يحتوي على الصورة بشكل صحيح في الرسائل ونافذة الملف الشخصي
- في `displayMessage()` السطر 1486-1496: الإطار يعمل لكن يحتاج تحسين ليظهر بشكل صحيح
- في `updateProfileSheet()` السطر 1575-1590: الإطار في نافذة البروفايل

### الحل:
- تحسين CSS لـ `.avatar-frame-container` و `.frame-overlay` ليضمن أن الصورة تكون داخل الإطار
- ضمان أن الإطار يغطي الصورة بشكل صحيح

## 2. الرسائل القديمة عند الخروج والرجوع (room.html)
### المشكلة:
- `setupListeners()` السطر 1283-1355: يقرأ الرسائل من Firestore
- عند الخروج والرجوع، الرسائل يجب أن تظهر من Firestore
- المشكلة في أن `displayMessage()` تحتاج sender data من `activeUsers` أو Firestore
- عندما يخرج المستخدم ويرجع، `activeUsers` فارغ فلا يجد بيانات المرسل

### الحل:
- تحسين `displayMessage()` لتحميل بيانات المرسل من Firestore إذا لم يكن في activeUsers
- ضمان أن الرسائل المحفوظة في Firestore تتضمن بيانات المرسل (الاسم، الصورة، الإطار، الشارات)

## 3. نافذة الداعمين (room.html)
### المشكلة:
- `loadGiftLeaders()` السطر 1514-1540: يحمل إحصائيات الهدايا
- `renderLeaderboard()` السطر 1542-1561: يعرض المتصدرين
- يعتمد على `activeUsers` للصور والأسماء - يحتاج تحسين

### الحل:
- تحميل بيانات الداعمين الحقيقيين من Firestore
- عرض عدد الدعم الفعلي لكل داعم

## 4. الغرف النشطة العامة (afnan_ai_white.html)
### المشكلة:
- `loadRooms()` السطر 513-525: يحمل الغرف من Firestore
- `renderRooms()` السطر 540-581: يعرض الغرف
- يحتاج إضافة عرض الغرف النشطة (التي فيها أشخاص) في تبويب "عام"
- يجب أن يستخدم `activeUsers` من RTDB لمعرفة الغرف النشطة

### الحل:
- تعديل loadRooms لتقرأ activeUsers من RTDB
- عرض الغرف النشطة في الأعلى مع عدد المستخدمين
- إمكانية المتابعة من الأعلى
